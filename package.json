{
  "private": true,
  "packageManager": "npm@9.0.0",
  "workspaces": [
    "ts-config",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.5.5"
  }
}
